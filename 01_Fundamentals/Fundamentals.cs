// Importa namespaces (bibliotecas) do .NET
using System;

// namespace — uma forma de organizar o códigozin
namespace ExemploBasico
{
    // Define uma classe — o bloco principal do nosso código
    class Program
    {
        // Ponto de entrada do programa (como o "main" em C ou Java)
        static void Main(string[] args)
        {
            // Imprime uma mensagem no console
            Console.WriteLine("Olá, mundo!");
        }
    }
}